Create a website using the code below: 
HTML:

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>CredAble – AI Operating System for Business Credit</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <!-- Inter Font -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">

  <link rel="stylesheet" href="./styles.css" />
</head>

<body>
  <canvas id="aiBg"></canvas>

  <header>
    <div class="nav">
      <div class="logo">
        <img src="300DPI_CredAble_Logo.png" alt="CredAble">
      </div>
      <div class="nav-actions">
        <button class="btn btn-primary" type="button">Launch Demo</button>
      </div>
    </div>
  </header>

  <main>
    <section class="hero">

      <!-- LEFT: AI CONSOLE -->
      <div class="console-card">

        <div class="console-header">
          <div class="console-dots">
            <span></span><span></span><span></span>
          </div>
          <span class="console-title">credable://ai-credit-engine</span>
        </div>

        <!-- AI HANDSHAKE -->
        <div class="ai-handshake" id="handshake">
          <div class="ai-handshake-inner ai-palette">

            <div class="ai-palette-top">
              <div class="ai-badge">AI READY</div>
              <div class="ai-palette-title">Instant credit recommendations.</div>
            </div>

            <div class="ai-palette-grid">
              <div class="ai-palette-copy">
                <div class="ai-palette-sub">
                  Type your company name to get <strong>sanction options</strong>, <strong>risk score</strong>, <strong>conditions</strong>, and <strong>red flags</strong> — instantly.
                </div>

                <div class="ai-palette-proof">
                  <span class="proof-pill">No signup</span>
                  <span class="proof-pill">Instant output</span>
                  <span class="proof-pill">Policy-linked</span>
                  <span class="proof-pill">Explainable AI</span>
                </div>
              </div>

              <div class="ai-palette-input">
                <label class="ai-label" for="companyInput">Company name</label>

                <div class="ai-input ai-input-compact">
                  <input id="companyInput" placeholder="e.g. Auto Parts Manufacturing Co" autocomplete="off" />
                  <button class="btn btn-primary" id="runAI" type="button">Run</button>
                </div>

                <div class="ai-live-hint" id="aiLiveHint">
                  Start typing — AI will surface live signals.
                </div>

                <div class="quick-picks quick-picks-compact">
                  <button type="button" data-name="Kirana Wholesale Traders Pvt Ltd">Kirana</button>
                  <button type="button" data-name="Auto Parts Manufacturing Co">Auto Parts</button>
                  <button type="button" data-name="Anchor Supplier Ltd">Anchor</button>
                </div>
              </div>
            </div>

          </div>
        </div>

        <!-- AI CONSOLE OUTPUT -->
        <div class="console-body" id="console"></div>

        <!-- Button shows AFTER analysis -->
        <button class="btn btn-ghost full-results-btn" id="openResults" type="button" style="display:none;">
          View Full Results
        </button>

        <!-- Decision strip always in console -->
        <div class="ai-result" id="resultStrip" style="display:none;">
          <div class="result-item">
            <div class="result-label">Decision</div>
            <div class="result-value" id="resDecision"></div>
          </div>
          <div class="result-item">
            <div class="result-label">Risk Score</div>
            <div class="result-value" id="resScore"></div>
          </div>
          <div class="result-item">
            <div class="result-label">Conditions</div>
            <div class="result-value" id="resConditions"></div>
          </div>
        </div>

      </div>

      <!-- RIGHT: MESSAGE -->
      <div class="hero-right">
        <div class="eyebrow">AI-Native Working Capital</div>
        <h1>CredAble is an AI Operating System for Business Credit.</h1>
        <p>
          Ingest unstructured financial data. Reason across cashflows, networks and policy.
          Generate lender-grade decisions in seconds.
        </p>
      </div>

    </section>
  </main>

  <!-- FULL RESULTS MODAL -->
  <div class="modal-backdrop" id="resultsModal" aria-hidden="true">
    <div class="modal" role="dialog" aria-modal="true" aria-label="Full AI Results">
      <div class="modal-header">
        <div class="modal-title">
          Full AI Results <span class="modal-sub" id="modalCompany"></span>
        </div>
        <button class="modal-close" id="closeResults" type="button">✕</button>
      </div>

      <div class="modal-body">
        <div class="modal-grid">

          <div class="modal-col">
            <div class="modal-card">
              <div class="card-title">Decision Summary</div>
              <div class="summary-row">
                <div class="summary-item">
                  <div class="result-label">Decision</div>
                  <div class="result-value" id="mDecision"></div>
                </div>
                <div class="summary-item">
                  <div class="result-label">Risk Score</div>
                  <div class="result-value" id="mScore"></div>
                </div>
                <div class="summary-item">
                  <div class="result-label">Conditions</div>
                  <div class="result-value" id="mConditions"></div>
                </div>
              </div>
            </div>

            <div class="modal-card" id="modalTimelineMount">
              <div class="card-title">Behaviour Over Time</div>
              <!-- timeline injected here -->
            </div>
          </div>

          <div class="modal-col">
            <div class="modal-card" id="modalOptionsMount">
              <div class="card-title">AI Recommended Structures</div>
              <!-- options injected here -->
            </div>

            <div class="modal-card">
              <div class="card-title">Explainability</div>
              <div class="explain-list" id="explainList"></div>
            </div>
          </div>

        </div>
      </div>
    </div>
  </div>

  <!-- Hidden mounts (rendered by JS, moved into modal on open) -->
  <div class="hidden-mount">
    <div class="ai-timeline" id="aiTimeline" style="display:none;">
      <div class="ai-timeline-head">
        <div class="ai-timeline-title">Behaviour over time</div>
        <div class="ai-timeline-sub">
          Past 6 months signals informing today’s decision (not a future loan forecast).
        </div>
      </div>

      <div class="ai-timeline-row" id="aiTimelineRow"></div>

      <div class="ai-timeline-detail" id="aiTimelineDetail">
        <div class="ai-timeline-detail-title" id="aiTLTitle">Hover a month</div>
        <div class="ai-timeline-detail-body" id="aiTLBody">
          See cashflow + compliance + concentration signals month-by-month.
        </div>
      </div>
    </div>

    <div class="ai-options" id="aiOptions" style="display:none;">
      <div class="ai-options-head">
        <div class="ai-options-title">AI Recommended Structures</div>
        <div class="ai-options-sub">Three sanction structures ranked by risk-adjusted fit.</div>
      </div>

      <div class="ai-options-grid" id="aiOptionsGrid"></div>

      <div class="ai-options-foot">
        <span class="ai-options-note">All recommendations are explainable and policy-linked.</span>
      </div>
    </div>
  </div>

  <script src="./app.js" defer></script>
</body>
</html>


CSS:

:root{
  --dark-grey:#1c1f24;
  --text-muted:#6b7280;
  --border-soft: rgba(15,23,42,0.10);
  --bg-soft:#f7f7f9;
  --true-yellow:#f4c542;
  --koi-orange:#f7931e;
}

*{ box-sizing:border-box; }
body{
  margin:0;
  font-family: Inter, system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
  color: var(--dark-grey);
  overflow-x:hidden;
}
#aiBg{ position:fixed; inset:0; z-index:-1; }

header{ padding: 14px 18px; }
.nav{
  display:flex; justify-content:space-between; align-items:center;
}
.logo img{ height:26px; }

.btn{
  border: 1px solid transparent;
  padding: 10px 14px;
  border-radius: 999px;
  cursor:pointer;
  font-weight:600;
}
.btn-primary{
  background: linear-gradient(135deg,var(--true-yellow),var(--koi-orange));
  color:#fff;
  box-shadow: 0 10px 18px rgba(247,147,30,0.22);
}
.btn-ghost{
  background: rgba(255,255,255,0.20);
  border: 1px solid rgba(255,255,255,0.24);
  color: #fff;
}

.hero{
  display:grid;
  grid-template-columns: 520px 1fr;
  gap: 24px;
  padding: 16px 18px 32px;
  align-items:start;
}

.console-card{
  background: rgba(10,12,18,0.88);
  border-radius: 26px;
  border: 1px solid rgba(255,255,255,0.08);
  padding: 14px;
  position: relative;
  overflow: hidden;
  color: #f5f5fb;
  min-height: 520px;
}

.console-header{
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap: 10px;
  padding: 6px 6px 10px;
  border-bottom: 1px solid rgba(255,255,255,0.08);
}
.console-title{ opacity:0.85; }
.console-dots span{
  display:inline-block; width:10px; height:10px; border-radius:50%;
  margin-right:6px; opacity:0.9;
}
.console-dots span:nth-child(1){ background:#ff5f57; }
.console-dots span:nth-child(2){ background:#febc2e; }
.console-dots span:nth-child(3){ background:#28c840; }

.console-body{
  margin-top: 10px;
  max-height: 260px;
  overflow:auto;
  padding-right: 6px;
}
.console-line{
  font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
  font-size: 0.85rem;
  line-height: 1.55;
}
.console-arrow{ color:#42c5f5; margin-right:6px; }
.good{ color:#5fe1a0; }
.bad{ color:#ff8b8b; }
.strong{ color:#ffe547; }

.ai-handshake{
  position:absolute;
  inset: 56px 14px 14px 14px;
  background: rgba(255,255,255,0.94);
  color: var(--dark-grey);
  border-radius: 18px;
  border: 1px solid rgba(0,0,0,0.06);
  box-shadow: 0 18px 44px rgba(0,0,0,0.22);
  z-index: 5;
}

.ai-handshake-inner{ padding: 14px; }

.ai-badge{
  font-size: 0.68rem;
  letter-spacing: 0.16em;
  text-transform: uppercase;
  background: linear-gradient(135deg,var(--true-yellow),var(--koi-orange));
  color:#fff;
  padding: 6px 10px;
  border-radius: 999px;
}

.ai-palette-top{
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap:10px;
  margin-bottom: 10px;
}
.ai-palette-title{ font-weight:600; }

.ai-palette-grid{
  display:grid;
  grid-template-columns: 1.05fr 0.95fr;
  gap: 14px;
}

.ai-palette-sub{ color: var(--text-muted); font-size:0.88rem; line-height:1.35; }
.ai-palette-proof{ display:flex; flex-wrap:wrap; gap:8px; margin-top:10px; }

.proof-pill{
  font-size:0.68rem;
  letter-spacing:0.12em;
  text-transform:uppercase;
  color: var(--text-muted);
  background: #fff;
  border: 1px solid var(--border-soft);
  padding: 6px 10px;
  border-radius: 999px;
}

.ai-label{
  display:block;
  font-size:0.72rem;
  letter-spacing:0.12em;
  text-transform:uppercase;
  color: var(--text-muted);
  margin-bottom:6px;
}

.ai-input-compact{ display:flex; gap:10px; }
.ai-input-compact input{
  flex:1;
  padding: 10px 12px;
  border-radius: 999px;
  border: 1px solid var(--border-soft);
  font-size: 0.92rem;
}

.ai-live-hint{ margin-top:8px; font-size:0.78rem; color: var(--text-muted); }
.quick-picks-compact{ display:flex; gap:8px; flex-wrap:wrap; margin-top:10px; }
.quick-picks-compact button{
  padding: 8px 10px;
  border-radius: 999px;
  border: 1px solid var(--border-soft);
  background: rgba(247,247,249,0.9);
  cursor:pointer;
}

.ai-result{
  margin-top: 12px;
  background: rgba(255,255,255,0.95);
  border-radius: 16px;
  border: 1px solid var(--border-soft);
  padding: 10px;
  display:flex;
  gap: 10px;
  color: var(--dark-grey);
}
.result-item{ flex:1; min-width:0; }
.result-label{
  font-size: 0.72rem;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: var(--text-muted);
}
.result-value{ font-weight:600; }

.full-results-btn{
  margin-top: 10px;
  width: 100%;
  border-radius: 14px;
}

/* Hidden mounts (timeline + options live here until modal open) */
.hidden-mount{ display:none; }

/* ===== AI Options ===== */
.ai-options{
  background: rgba(255,255,255,0.98);
  border: 1px solid var(--border-soft);
  border-radius: 18px;
  padding: 12px;
  color: var(--dark-grey);
}
.ai-options-grid{
  display:grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 10px;
  margin-top: 10px;
}
.ai-option{
  border-radius: 14px;
  border: 1px solid var(--border-soft);
  background:#fff;
  padding: 10px;
}
.ai-option-rank{
  font-size: 0.7rem;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: var(--text-muted);
  display:flex;
  justify-content:space-between;
  margin-bottom: 8px;
}
.ai-pill{
  font-size: 0.7rem;
  padding: 4px 8px;
  border-radius: 999px;
  background: var(--bg-soft);
  border: 1px solid var(--border-soft);
}
.ai-option-name{ font-weight: 700; margin-bottom: 6px; }
.ai-option-lines{ color: var(--text-muted); font-size: 0.82rem; display:flex; flex-direction:column; gap:3px; }

/* ===== Timeline ===== */
.ai-timeline{
  background: rgba(255,255,255,0.06);
  border: 1px solid rgba(255,255,255,0.12);
  border-radius: 18px;
  padding: 12px;
  color: #f5f5fb;
}
.ai-timeline-head{
  display:flex;
  justify-content:space-between;
  gap: 10px;
  margin-bottom: 10px;
}
.ai-timeline-title{ font-weight:700; font-size: 0.9rem; }
.ai-timeline-sub{ font-size: 0.75rem; opacity:0.75; text-align:right; }
.ai-timeline-row{
  position: relative;
  display:flex;
  flex-direction:column;
  gap:18px;
  margin: 8px 0 14px;
  padding: 6px 0 0;
}
.tl-expand{
  align-self:flex-start;
  padding: 10px 14px;
  border-radius: 12px;
  border: 1px solid rgba(255,255,255,0.16);
  background: rgba(255,255,255,0.12);
  color: #f5f5fb;
  cursor:pointer;
  font-weight:700;
  letter-spacing:0.08em;
  text-transform:uppercase;
}
.tl-expand:hover{
  border-color: rgba(95,225,160,0.55);
  background: rgba(255,255,255,0.18);
}

.ai-timeline-row.expanded{
  flex-direction: row;
  flex-wrap: wrap;
  align-items: flex-end;
  gap: 14px;
  padding: 18px 6px 16px;
  overflow-x:auto;
}
.ai-timeline-row.expanded::before{
  content:"";
  position:absolute;
  left:0; right:0;
  height:2px;
  background: rgba(255,255,255,0.18);
  bottom:10px;
  pointer-events:none;
}

.ai-timeline-row:not(.expanded) .tl-node{ display:none; }
.ai-timeline-row.expanded .tl-expand{ display:none; }

.tl-node{
  position: relative;
  display:flex;
  flex-direction: column-reverse;
  align-items:center;
  gap: 12px;
  min-width: 160px;
  padding-bottom: 6px;
}
.tl-point{
  width: 12px;
  height: 12px;
  border-radius:50%;
  box-shadow: 0 0 0 6px rgba(255,255,255,0.08);
  background:#9aa3b2;
  position: relative;
  z-index:2;
}
.tl-node.good .tl-point{ background:#5fe1a0; box-shadow:0 0 0 6px rgba(95,225,160,0.16); }
.tl-node.warn .tl-point{ background:#ffe547; box-shadow:0 0 0 6px rgba(255,229,71,0.18); }
.tl-node.bad .tl-point{ background:#ff8b8b; box-shadow:0 0 0 6px rgba(255,139,139,0.22); }
.tl-card{
  background: rgba(255,255,255,0.10);
  border: 1px solid rgba(255,255,255,0.18);
  border-radius: 14px;
  padding: 10px 12px;
  box-shadow: 0 16px 40px rgba(0,0,0,0.20);
  width: 100%;
}
.tl-month{
  font-weight:800;
  letter-spacing:0.12em;
  text-transform:uppercase;
  margin-bottom: 6px;
}
.tl-lines{ opacity:0.9; font-size: 0.88rem; display:flex; flex-direction:column; gap:4px; }

.ai-timeline-detail{
  background: rgba(0,0,0,0.18);
  border: 1px solid rgba(255,255,255,0.10);
  border-radius: 14px;
  padding: 10px;
  overflow: hidden;
}
.ai-timeline-detail-title{ font-weight:700; margin-bottom: 6px; }
.ai-timeline-detail-body{
  opacity:0.85;
  font-size: 0.86rem;
  line-height:1.35;
  transition: transform .2s ease, opacity .2s ease;
}
.ai-timeline-detail-body.fade-out{
  opacity:0;
  transform: translateY(10px);
}
.ai-timeline-detail-body.fade-in{
  opacity:1;
  transform: translateY(0);
}

/* ===== Modal ===== */
.modal-backdrop{
  position: fixed;
  inset: 0;
  display: none;
  background: rgba(0,0,0,0.55);
  backdrop-filter: blur(10px);
  z-index: 999;
  padding: 18px;
}
.modal-backdrop.open{
  display:flex;
  align-items:center;
  justify-content:center;
}
.modal{
  width: min(1100px, 100%);
  max-height: 92vh;
  overflow: hidden;
  border-radius: 22px;
  background: rgba(255,255,255,0.97);
  border: 1px solid rgba(0,0,0,0.08);
  box-shadow: 0 24px 80px rgba(0,0,0,0.35);
  display:flex;
  flex-direction:column;
}
.modal-header{
  padding: 14px 16px;
  display:flex;
  align-items:center;
  justify-content:space-between;
  border-bottom: 1px solid var(--border-soft);
}
.modal-title{ font-weight:700; }
.modal-sub{ color: var(--text-muted); font-weight:500; margin-left: 8px; }
.modal-close{
  border: 1px solid var(--border-soft);
  background:#fff;
  border-radius: 12px;
  padding: 8px 10px;
  cursor:pointer;
}
.modal-body{ padding: 14px; overflow:auto; }
.modal-grid{
  display:grid;
  grid-template-columns: 1fr 1fr;
  gap: 14px;
}
.modal-card{
  background:#fff;
  border: 1px solid var(--border-soft);
  border-radius: 18px;
  padding: 12px;
}
.card-title{
  font-size: 0.78rem;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: var(--text-muted);
  margin-bottom: 10px;
}
.summary-row{
  display:grid;
  grid-template-columns: 1fr 1fr 1fr;
  gap: 10px;
}
.summary-item{
  background: var(--bg-soft);
  border: 1px solid var(--border-soft);
  border-radius: 16px;
  padding: 10px;
}
.explain-list{ display:flex; flex-direction:column; gap: 8px; }
.explain-item{
  padding: 10px;
  border-radius: 14px;
  border: 1px solid var(--border-soft);
  background: var(--bg-soft);
  color: var(--dark-grey);
  font-size: 0.9rem;
}

.hero-right{ padding-top: 8px; }
.eyebrow{
  font-size:0.72rem;
  letter-spacing:0.18em;
  text-transform:uppercase;
  color: rgba(20,30,50,0.62);
}
.hero-right h1{ margin: 10px 0 10px; font-size: 2.2rem; }
.hero-right p{ max-width: 560px; color: rgba(20,30,50,0.72); line-height: 1.6; }

@media (max-width: 980px){
  .hero{ grid-template-columns: 1fr; }
  .modal-grid{ grid-template-columns: 1fr; }
  .summary-row{ grid-template-columns: 1fr; }
  .ai-palette-grid{ grid-template-columns: 1fr; }
}

/* ===== Timeline: premium modal style ===== */
.modal-backdrop.open .ai-timeline{
  background: #fff !important;
  border: 1px solid var(--border-soft) !important;
  border-radius: 18px !important;
  padding: 14px !important;
  color: var(--dark-grey) !important;
}

/* Header */
.modal-backdrop.open .ai-timeline-head{
  display:flex !important;
  align-items: flex-start !important;
  justify-content: space-between !important;
  gap: 12px !important;
  margin-bottom: 12px !important;
}

.modal-backdrop.open .ai-timeline-title{
  font-weight: 700 !important;
  font-size: 0.95rem !important;
  letter-spacing: 0.14em !important;
  text-transform: uppercase !important;
  color: rgba(20,30,50,0.72) !important;
}

.modal-backdrop.open .ai-timeline-sub{
  font-size: 0.82rem !important;
  color: var(--text-muted) !important;
  opacity: 1 !important;
  text-align: right !important;
  max-width: 360px;
}

/* Month chips row */
.modal-backdrop.open .ai-timeline-row{
  display:flex !important;
  flex-direction: row !important;
  flex-wrap: wrap !important;
  align-items: flex-end !important;
  gap: 14px !important;
  margin: 6px 0 14px !important;
  padding: 18px 6px 16px !important;
}
.modal-backdrop.open .ai-timeline-row::before{
  background: rgba(15,23,42,0.12) !important;
}

.modal-backdrop.open .tl-expand{
  border-color: var(--border-soft) !important;
  background: #fff !important;
  color: var(--dark-grey) !important;
}

/* Detail card */
.modal-backdrop.open .ai-timeline-detail{
  background: linear-gradient(180deg, rgba(247,247,249,0.9), rgba(247,247,249,0.75)) !important;
  border: 1px solid var(--border-soft) !important;
  border-radius: 16px !important;
  padding: 14px !important;
  overflow: hidden !important;
}

.modal-backdrop.open .ai-timeline-detail-title{
  font-weight: 800 !important;
  font-size: 1.05rem !important;
  margin-bottom: 10px !important;
  color: var(--dark-grey) !important;
}

.modal-backdrop.open .ai-timeline-detail-body{
  color: rgba(20,30,50,0.76) !important;
  opacity: 1 !important;
  font-size: 0.95rem !important;
  line-height: 1.55 !important;
}

/* Make bullet dots nicer */
.modal-backdrop.open .ai-timeline-detail-body strong{
  color: rgba(20,30,50,0.92) !important;
}

/* Ensure button is clickable above overlays */
#openResults{
  position: relative;
  z-index: 50;
  pointer-events: auto;
}

/* When handshake is present/hidden, it should not block clicks below */
#handshake{
  pointer-events: auto;
}
#handshake[style*="display: none"]{
  pointer-events: none;
}


JS:

// ===== Hero AI Console =====
const consoleEl = document.getElementById("console");
const handshake = document.getElementById("handshake");
const runBtn = document.getElementById("runAI");
const input = document.getElementById("companyInput");
const resultStrip = document.getElementById("resultStrip");

const resDecision = document.getElementById("resDecision");
const resScore = document.getElementById("resScore");
const resConditions = document.getElementById("resConditions");

let liveTimer = null;
let lastText = "";

// ===== Modal elements =====
const openResultsBtn = document.getElementById("openResults");
const resultsModal = document.getElementById("resultsModal");
const closeResultsBtn = document.getElementById("closeResults");
const modalCompany = document.getElementById("modalCompany");

const mDecision = document.getElementById("mDecision");
const mScore = document.getElementById("mScore");
const mConditions = document.getElementById("mConditions");

const modalTimelineMount = document.getElementById("modalTimelineMount");
const modalOptionsMount = document.getElementById("modalOptionsMount");
const explainList = document.getElementById("explainList");

let lastCompanyForModal = "";

// ===== Hidden result mounts =====
const aiTimeline = document.getElementById("aiTimeline");
const aiTimelineRow = document.getElementById("aiTimelineRow");
const aiTLTitle = document.getElementById("aiTLTitle");
const aiTLBody = document.getElementById("aiTLBody");

const aiOptionsEl = document.getElementById("aiOptions");
const aiOptionsGrid = document.getElementById("aiOptionsGrid");

// ===== Helpers =====
function escapeHtml(s) {
  return String(s)
    .replaceAll("&", "&amp;")
    .replaceAll("<", "&lt;")
    .replaceAll(">", "&gt;")
    .replaceAll('"', "&quot;")
    .replaceAll("'", "&#039;");
}

function log(text, cls = "") {
  const div = document.createElement("div");
  div.className = "console-line";
  div.innerHTML = `<span class="console-arrow">→</span> <span class="${cls}">${escapeHtml(text)}</span>`;
  consoleEl.appendChild(div);
  consoleEl.scrollTop = consoleEl.scrollHeight;
}

function resetToListening() {
  consoleEl.innerHTML = "";
  resultStrip.style.display = "none";
  openResultsBtn.style.display = "none";
  log("AI status: active", "good");
  log("Waiting for business input…");
}

function hash(str) {
  let h = 0;
  for (let i = 0; i < str.length; i++) {
    h = (h << 5) - h + str.charCodeAt(i);
    h |= 0;
  }
  return Math.abs(h);
}

function clamp(n, a, b) { return Math.max(a, Math.min(b, n)); }

// ===== Live preview =====
function previewSignals(name) {
  const h = hash(name);
  const revenue = (h % 18) - 7;          // -7..+10
  const concentration = 30 + (h % 55);   // 30..84
  const volatility = 8 + (h % 22);       // 8..30
  const dscr = (90 + (h % 45)) / 100;    // 0.90..1.34

  log(`Live signals — Revenue trend: ${revenue}%`, revenue < 0 ? "bad" : "good");
  log(`Live signals — Counterparty concentration: ${concentration}%`, concentration > 65 ? "bad" : "strong");
  log(`Live signals — Cashflow volatility index: ${volatility}`, volatility > 22 ? "bad" : "strong");
  log(`Live signals — DSCR estimate: ${dscr.toFixed(2)}`, dscr < 1.1 ? "bad" : "good");
}

// ===== Options render =====
function renderOptions(options) {
  aiOptionsGrid.innerHTML = "";
  options.forEach((o, idx) => {
    const card = document.createElement("div");
    card.className = "ai-option";
    card.innerHTML = `
      <div class="ai-option-rank">
        <span>Option ${idx + 1}</span>
        <span class="ai-pill">${escapeHtml(o.tag)}</span>
      </div>
      <div class="ai-option-name">${escapeHtml(o.title)}</div>
      <div class="ai-option-lines">
        <div><strong>Limit:</strong> ${escapeHtml(o.limit)}</div>
        <div><strong>Tenor:</strong> ${escapeHtml(o.tenor)}</div>
        <div><strong>Pricing:</strong> ${escapeHtml(o.pricing)}</div>
        <div><strong>Controls:</strong> ${escapeHtml(o.controls)}</div>
      </div>
    `;
    aiOptionsGrid.appendChild(card);
  });
  aiOptionsEl.style.display = "block";
}

// ===== Timeline render =====
function monthLabelsLast6() {
  const labels = [];
  const d = new Date();
  d.setDate(1);
  for (let i = 5; i >= 0; i--) {
    const m = new Date(d.getFullYear(), d.getMonth() - i, 1);
    labels.push(m.toLocaleString(undefined, { month: "short" }).toUpperCase());
  }
  return labels;
}

function buildTimeline(company) {
  const labels = monthLabelsLast6();
  const h = hash(company);

  return labels.map((lab, idx) => {
    const seed = (h + idx * 9973) % 100000;

    const inflowTrend = clamp((seed % 29) - 14, -18, 18);
    const gstDelayDays = seed % 3 === 0 ? seed % 11 : 0;
    const concentration = clamp(35 + (seed % 55), 30, 90);
    const volatility = clamp(10 + (seed % 25), 8, 35);

    let status = "good";
    if (gstDelayDays >= 7 || concentration >= 70 || volatility >= 26 || inflowTrend <= -10) status = "bad";
    else if (gstDelayDays > 0 || concentration >= 60 || volatility >= 22 || inflowTrend < 0) status = "warn";

    return { label: lab, status, inflowTrend, gstDelayDays, concentration, volatility };
  });
}

function renderTimeline(company) {
  const months = buildTimeline(company);
  aiTimelineRow.innerHTML = "";
  aiTimelineRow.classList.remove("expanded");
  aiTLBody.classList.remove("timeline-expanded");

  const toggleBtn = document.createElement("button");
  toggleBtn.className = "tl-expand";
  toggleBtn.type = "button";
  toggleBtn.textContent = "View full timeline";
  aiTimelineRow.appendChild(toggleBtn);

  const makeCard = (m) => {
    const wrap = document.createElement("div");
    wrap.className = `tl-node ${m.status}`;
    wrap.innerHTML = `
      <div class="tl-point"></div>
      <div class="tl-card">
        <div class="tl-month">${m.label}</div>
        <div class="tl-lines">
          <div>Cash inflow trend: <strong>${m.inflowTrend}%</strong></div>
          <div>GST compliance: <strong>${m.gstDelayDays === 0 ? "On-time" : `${m.gstDelayDays} day delay`}</strong></div>
          <div>Concentration: <strong>${m.concentration}%</strong></div>
          <div>Volatility index: <strong>${m.volatility}</strong></div>
        </div>
      </div>
    `;
    return wrap;
  };

  const renderExpanded = () => {
    aiTimelineRow.classList.add("expanded");
    aiTimelineRow.innerHTML = "";
    months.forEach((m) => aiTimelineRow.appendChild(makeCard(m)));
    aiTLTitle.textContent = "Behaviour over time — full view";
    aiTLBody.innerHTML = "Behaviour-over-time informing today’s decision.";
    aiTLBody.classList.add("timeline-expanded");
  };

  toggleBtn.addEventListener("click", renderExpanded);

  aiTimeline.style.display = "block";
}

// ===== Explainability render =====
function renderExplainability(mode) {
  const items =
    mode === 0 ? [
      { t: "Stable inflows over 6 months", s: "+9%" },
      { t: "Concentration within policy threshold", s: "+6%" },
      { t: "Low volatility & consistent GST compliance", s: "+5%" },
      { t: "No round-tripping detected", s: "+3%" },
    ] :
    mode === 1 ? [
      { t: "Moderate concentration in top buyers", s: "−9%" },
      { t: "DSCR close to policy floor", s: "−7%" },
      { t: "GST timing variance detected", s: "−4%" },
      { t: "Mitigated by escrow + invoice assignment", s: "+6%" },
    ] : [
      { t: "High volatility in cashflows", s: "−12%" },
      { t: "Related-party transfer patterns", s: "−10%" },
      { t: "Concentration above policy threshold", s: "−8%" },
      { t: "Insufficient explainable buffers", s: "−6%" },
    ];

  explainList.innerHTML = "";
  items.forEach(x => {
    const div = document.createElement("div");
    div.className = "explain-item";
    div.innerHTML = `<strong>${escapeHtml(x.s)}</strong> — ${escapeHtml(x.t)}`;
    explainList.appendChild(div);
  });
}

// ===== Modal logic =====
// ===== Modal logic (single source of truth) =====
function openModal() {
  modalCompany.textContent = lastCompanyForModal ? `— ${lastCompanyForModal}` : "";
  mDecision.textContent = resDecision.textContent;
  mScore.textContent = resScore.textContent;
  mConditions.textContent = resConditions.textContent;

  // Move nodes into modal
  modalTimelineMount.appendChild(aiTimeline);
  modalOptionsMount.appendChild(aiOptionsEl);

  // Force visible inside modal
  aiTimeline.style.display = "block";
  aiOptionsEl.style.display = "block";

  resultsModal.classList.add("open");
  resultsModal.setAttribute("aria-hidden", "false");
}

function closeModal() {
  resultsModal.classList.remove("open");
  resultsModal.setAttribute("aria-hidden", "true");
}

window.addEventListener("load", () => {
  if (!openResultsBtn || !resultsModal) {
    console.error("Missing modal elements", { openResultsBtn, resultsModal });
    return;
  }

  // Ensure button can be clicked above overlays
  openResultsBtn.style.position = "relative";
  openResultsBtn.style.zIndex = "50";
  openResultsBtn.style.pointerEvents = "auto";

  openResultsBtn.addEventListener("click", (e) => {
    e.preventDefault();
    openModal();
  });

  closeResultsBtn?.addEventListener("click", closeModal);

  resultsModal.addEventListener("click", (e) => {
    if (e.target === resultsModal) closeModal();
  });

  window.addEventListener("keydown", (e) => {
    if (e.key === "Escape" && resultsModal.classList.contains("open")) closeModal();
  });
});

// ===== Main analysis run =====
function runAI(name) {
  const company = (name || "Sample Manufacturing Pvt Ltd").trim();

  consoleEl.innerHTML = "";
  handshake.style.display = "none";
  handshake.style.pointerEvents = "none";
  openResultsBtn.style.display = "none";
  resultStrip.style.display = "none";

  // Ensure hidden mounts are reset
  aiTimeline.style.display = "none";
  aiOptionsEl.style.display = "none";

  log(`Starting analysis: ${company}`, "strong");
  log("Loading policy pack: SME + Supply Chain");
  log("Building financial reasoning graph…");

  setTimeout(() => log("Parsing bank statement…"), 450);
  setTimeout(() => log("Reconciling GST filings…"), 900);
  setTimeout(() => log("Mapping director network…", "strong"), 1350);

  setTimeout(() => {
    const h = hash(company);
    const mode = h % 3; // 0 approve, 1 conditional, 2 decline
    let options = [];

    if (mode === 0) {
      options = [
        { tag:"Best fit", title:"Working Capital OD", limit:"₹50L", tenor:"12 months", pricing:"MCLR + 2.25%", controls:"Standard covenants" },
        { tag:"Lower risk", title:"OD + Escrow", limit:"₹60L", tenor:"12 months", pricing:"MCLR + 2.00%", controls:"Escrow on top 3 buyers" },
        { tag:"Faster drawdowns", title:"Invoice Finance Line", limit:"₹40L", tenor:"90–120 days", pricing:"1.2% per 30 days", controls:"Invoice assignment + anchor validation" },
      ];
      resDecision.textContent = "APPROVE";
      resScore.textContent = "742 (Low)";
      resConditions.textContent = "Standard covenants";
      log("Decision: APPROVE", "good");
    }

    if (mode === 1) {
      options = [
        { tag:"Best fit", title:"OD with Controls", limit:"₹45L", tenor:"12 months", pricing:"MCLR + 2.75%", controls:"Escrow + invoice assignment" },
        { tag:"Lower risk", title:"Invoice Finance (Anchor-led)", limit:"₹55L", tenor:"90 days", pricing:"1.35% per 30 days", controls:"Anchor confirmation + concentration cap" },
        { tag:"Conservative", title:"Term Loan Lite", limit:"₹30L", tenor:"24 months", pricing:"MCLR + 3.10%", controls:"DSCR covenant + quarterly review" },
      ];
      resDecision.textContent = "CONDITIONAL APPROVAL";
      resScore.textContent = "712 (Moderate-Low)";
      resConditions.textContent = "Escrow + Invoice Assignment";
      log("Decision: CONDITIONAL APPROVAL", "strong");
      log("Conditions: Escrow + invoice assignment", "strong");
    }

    if (mode === 2) {
      options = [
        { tag:"If reconsidered", title:"Small OD + Hard Controls", limit:"₹15L", tenor:"6 months", pricing:"MCLR + 4.25%", controls:"100% escrow + weekly monitoring" },
        { tag:"Alternative", title:"Secured Against FD", limit:"₹25L", tenor:"12 months", pricing:"FD rate + 2.0%", controls:"Lien on FD + auto-sweep" },
        { tag:"Non-fund", title:"LC/BG Only", limit:"₹20L", tenor:"Per transaction", pricing:"As per schedule", controls:"Collateral + margin + approvals" },
      ];
      resDecision.textContent = "DECLINE";
      resScore.textContent = "608 (Elevated)";
      resConditions.textContent = "Related-party + volatility flags";
      log("Decision: DECLINE", "bad");
      log("Reason: Related-party risk + cashflow volatility", "bad");
    }

    log("Generating AI recommended sanction structures…", "strong");

    setTimeout(() => {
      renderOptions(options);
      renderTimeline(company);
      renderExplainability(mode);

      // Show only compact summary on landing
      resultStrip.style.display = "flex";
      openResultsBtn.style.display = "block";

      // Keep heavy UI in modal (hide inline)
      aiTimeline.style.display = "none";
      aiOptionsEl.style.display = "none";

      lastCompanyForModal = company;
      log("Results ready. Open Full Results for complete output.", "good");
    }, 650);

  }, 1900);
}

// ===== Live typing =====
input.addEventListener("input", () => {
  const v = input.value.trim();
  if (v === lastText) return;
  lastText = v;

  if (liveTimer) clearTimeout(liveTimer);

  liveTimer = setTimeout(() => {
    resetToListening();
    if (v.length >= 3) {
      log(`Interpreting: "${v}"`, "strong");
      previewSignals(v);
      log("Press Enter to run full decision.", "strong");
    } else {
      log("Type 3+ characters to surface live signals…");
    }
  }, 250);
});

input.addEventListener("keydown", (e) => {
  if (e.key === "Enter") {
    e.preventDefault();
    runAI(input.value);
  }
});

runBtn.addEventListener("click", () => runAI(input.value));

document.querySelectorAll(".quick-picks button").forEach((b) => {
  b.addEventListener("click", () => {
    input.value = b.dataset.name || "";
    runAI(input.value);
  });
});

// Init
window.addEventListener("load", () => {
  resetToListening();
  setTimeout(() => input.focus(), 250);
});

// ===== Moving AI background (nodes + links) =====
const canvas = document.getElementById("aiBg");
const ctx = canvas.getContext("2d");
let W, H, DPR;

function resize() {
  DPR = Math.max(1, Math.floor(window.devicePixelRatio || 1));
  W = canvas.width = Math.floor(window.innerWidth * DPR);
  H = canvas.height = Math.floor(window.innerHeight * DPR);
  canvas.style.width = window.innerWidth + "px";
  canvas.style.height = window.innerHeight + "px";
}
window.addEventListener("resize", resize);
resize();

const N = 70;
const nodes = Array.from({ length: N }).map(() => ({
  x: Math.random() * W,
  y: Math.random() * H,
  vx: (Math.random() - 0.5) * 0.35 * DPR,
  vy: (Math.random() - 0.5) * 0.35 * DPR,
  r: (Math.random() * 1.6 + 0.6) * DPR,
}));

function tick() {
  ctx.clearRect(0, 0, W, H);

  for (let i = 0; i < N; i++) {
    const a = nodes[i];
    a.x += a.vx; a.y += a.vy;
    if (a.x < 0 || a.x > W) a.vx *= -1;
    if (a.y < 0 || a.y > H) a.vy *= -1;

    for (let j = i + 1; j < N; j++) {
      const b = nodes[j];
      const dx = a.x - b.x, dy = a.y - b.y;
      const d2 = dx * dx + dy * dy;
      const max = (160 * DPR) * (160 * DPR);

      if (d2 < max) {
        const t = 1 - d2 / max;
        ctx.globalAlpha = 0.20 * t;
        ctx.beginPath();
        ctx.moveTo(a.x, a.y);
        ctx.lineTo(b.x, b.y);
        ctx.lineWidth = 1 * DPR;
        ctx.strokeStyle = "rgba(40,60,90,1)";
        ctx.stroke();
      }
    }
  }

  ctx.globalAlpha = 0.55;
  for (const n of nodes) {
    ctx.beginPath();
    ctx.arc(n.x, n.y, n.r, 0, Math.PI * 2);
    ctx.fillStyle = "rgba(20,30,50,1)";
    ctx.fill();
  }

  ctx.globalAlpha = 1;
  requestAnimationFrame(tick);
}
tick();